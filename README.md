# claims-app
Claims Service — Fullstack Serverless App (Vite + AWS + TypeScript)

This repository contains a full-stack serverless application for managing Projects and Claims.
The backend runs on AWS Lambda, API Gateway, and DynamoDB, while the frontend is a React + TypeScript Vite app that interacts with the API.